[build]
  command = "npx playwright install chromium && node qa/netlify-probe.mjs"
  publish = "qa-public"

[build.environment]
  NODE_VERSION = "22"
  PLAYWRIGHT_BROWSERS_PATH = "0"
  PROBE = "browser_initial"
