# TEMPORARY, UNMERGED JANE GAME BRANCH. Never merge into GiB main.
# The build refuses production context and publishes only jane-game-play.
[build]
  command = "node jane-game-publish/bootstrap.mjs"
  publish = "jane-game-artifact/dist"
  functions = "jane-game-artifact/netlify/functions"
[build.environment]
  NODE_VERSION = "22"
[functions]
  node_bundler = "esbuild"
