name: Oil Atlas Motion Frames

on:
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/oil-atlas-motion-frames.yml"

permissions:
  contents: read

env:
  PREVIEW_URL: https://deploy-preview-${{ github.event.pull_request.number }}--gib-live.netlify.app

jobs:
  capture:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4

      - name: Wait for the current Netlify preview
        run: |
          for attempt in $(seq 1 84); do
            if curl --fail --silent --location "$PREVIEW_URL/" -o /tmp/atlas.html && grep -q 'src="/presentation.js"' /tmp/atlas.html; then
              exit 0
            fi
            sleep 5
          done
          exit 1

      - name: Capture the right side at normal speed
        run: |
          mkdir -p /tmp/oil-atlas-motion/frames
          google-chrome --headless=new --no-sandbox --disable-gpu --disable-dev-shm-usage --hide-scrollbars \
            --remote-debugging-port=9224 --user-data-dir=/tmp/oil-atlas-motion-chrome about:blank \
            >/tmp/oil-atlas-motion-chrome.log 2>&1 &
          echo $! >/tmp/oil-atlas-motion-chrome.pid

          node --input-type=module <<'NODE'
          import { writeFileSync } from 'node:fs';
          const sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms));
          const start = Date.parse('2025-09-01T00:00:00Z');
          const dayIndex = (iso) => Math.round((Date.parse(`${iso}T00:00:00Z`) - start) / 86400000);

          let page;
          for (let i = 0; i < 120; i += 1) {
            try {
              const tabs = await (await fetch('http://127.0.0.1:9224/json/list')).json();
              page = tabs.find((tab) => tab.type === 'page');
              if (page?.webSocketDebuggerUrl) break;
            } catch {}
            await sleep(250);
          }
          if (!page?.webSocketDebuggerUrl) {
            try { console.error(await (await import('node:fs/promises')).readFile('/tmp/oil-atlas-motion-chrome.log', 'utf8')); } catch {}
            throw new Error('Chrome unavailable');
          }

          const ws = new WebSocket(page.webSocketDebuggerUrl);
          await new Promise((resolve, reject) => {
            ws.addEventListener('open', resolve, { once: true });
            ws.addEventListener('error', reject, { once: true });
          });
          let nextId = 0;
          const pending = new Map();
          ws.addEventListener('message', (event) => {
            const message = JSON.parse(event.data);
            if (!message.id) return;
            const waiter = pending.get(message.id);
            if (!waiter) return;
            pending.delete(message.id);
            if (message.error) waiter.reject(new Error(JSON.stringify(message.error)));
            else waiter.resolve(message.result);
          });
          const send = (method, params = {}) => new Promise((resolve, reject) => {
            const id = ++nextId;
            pending.set(id, { resolve, reject });
            ws.send(JSON.stringify({ id, method, params }));
          });
          const evaluate = async (expression) => {
            const result = await send('Runtime.evaluate', { expression, awaitPromise: true, returnByValue: true });
            if (result.exceptionDetails) throw new Error(JSON.stringify(result.exceptionDetails));
            return result.result.value;
          };

          await send('Page.enable');
          await send('Runtime.enable');
          await send('Emulation.setDeviceMetricsOverride', { width: 1366, height: 768, deviceScaleFactor: 1, mobile: false });
          await send('Page.navigate', { url: process.env.PREVIEW_URL });
          await sleep(7000);
          await evaluate(`(async () => {
            const button = document.getElementById('playButton');
            if (button && /Pause/i.test(button.textContent || '')) button.click();
            await new Promise((resolve) => setTimeout(resolve, 200));
            [...document.querySelectorAll('button')].find((node) => /^1[×x]$/i.test((node.textContent || '').trim()))?.click();
            const slider = document.getElementById('timeline');
            slider.value = '${dayIndex('2026-01-15')}';
            slider.dispatchEvent(new Event('input', { bubbles: true }));
            slider.dispatchEvent(new Event('change', { bubbles: true }));
            await new Promise((resolve) => setTimeout(resolve, 900));
            if (button && /Play/i.test(button.textContent || '')) button.click();
            await new Promise((resolve) => setTimeout(resolve, 800));
          })()`);

          const clip = { x: 770, y: 0, width: 596, height: 768, scale: 1 };
          const captureStart = performance.now();
          for (let frame = 0; frame < 120; frame += 1) {
            const shot = await send('Page.captureScreenshot', { format: 'jpeg', quality: 82, fromSurface: true, clip });
            writeFileSync(`/tmp/oil-atlas-motion/frames/frame-${String(frame).padStart(4, '0')}.jpg`, Buffer.from(shot.data, 'base64'));
            const target = captureStart + (frame + 1) * 66.6667;
            await sleep(Math.max(0, target - performance.now()));
          }
          ws.close();
          NODE

          kill "$(cat /tmp/oil-atlas-motion-chrome.pid)" || true

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: oil-atlas-right-motion-frames
          path: /tmp/oil-atlas-motion/frames
          if-no-files-found: error
          retention-days: 7
