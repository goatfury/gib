# Promotions TEST handoff

Status: implementation and simulated integration checks pass; **the hosted Google workflow is not verified or ready for owner review**. The separately frozen kiosk hotfix in PR #84 was explicitly authorized and merged as `1f16dd6ac2e5a1c974b50a98d14f31dbc2453b31`; both gyms have published it, with final release verification recorded in PR #84. Keep both released kiosks untouched. Promotions PR #85 remains draft, unmerged, separate, and TEST only.

Application: `f00bea9b2bf225bce311d2f6b680088f31452bd8` on `prototype/promotions-log-test-20260913`, based on main `93dba9e`. Core files are `promotions/Code.gs`, `promotions/Index.html`, and `promotions/appsscript.json`. No production deployment or live migration is authorized.

## Implemented

The protected TEST flow finds a student, previews one stripe/degree or a belt change, and records the promotion with its New York date, before/after rank, fictional approver, authenticated recorder, and permanent event/request identities. Missing students require explicit registration; unknown ranks require explicit confirmation; identical names retain separate identifying labels. Corrections append an audit entry and preserve the original.

`Promotion History` is the sole rank authority. `Students` is a derived view. A shared save lock, exact request fingerprints, and expected revisions prevent duplicate retries and stale concurrent awards. Lost responses retain the same request; recovery checks repair the derived view or report it pending. Late callbacks preserve another student's input and cannot restore a submitted draft or downgrade a confirmed rank.

The manifest specifies Google access `MYSELF` and execution `USER_ACCESSING`. Both the page and every callable data operation require nonblank active and effective Google identities matching the privately configured owner. This has automated coverage; real hosted enforcement remains unverified. Selecting an approver is explicitly not authentication or proof of approval. Production entry/approval policy remains an owner decision.

## Verified evidence

- Final automated run: **848 passed, zero failures/skips**, including **32 promotions tests**. This also covers the retained M1 baseline. Command: `npm test`. After that run, Google rejected a numeric separator in the server lock timeout; the equivalent literal was corrected, all 32 promotions tests passed again, and the actual Google editor accepted the saved backend. The interface is unchanged from the browser-tested revision.
- Actual Chromium with simulated `google.script.run`: **16 final-source workflow groups passed**, including duplicate submissions, missing/unknown students, failure recovery, late replies, and corrected callback ordering. Phone 390px, tablet 820px, and laptop 1440px layouts were visually inspected; suggestions stay bounded and controls are at least 48px. The unchanged request timeout was exercised for approximately 30 seconds. These checks do not prove Google authentication or durable backend writes.
- The separate private synthetic workbook contains **8 baseline student/history records** and **6 preserved legacy tabs**. Local fixture renders and native data readback were checked. **Native baseline visual sanity passed** in Google Sheets under the exact Ops account: all eight tabs were rendered and visually inspected at laptop width, including headers and visible populated fixture rows. Duplicate names, unknown rank, blanks, `?`, `Transplant`, and `Early 2014` annotations remain preserved. This was read-only baseline inspection; no cells were edited. After-save native visual QA remains pending. No actual promotion has yet been verified through the hosted application and an independent persisted readback.

Task-private evidence remains outside the repository: `work/promotions-final-tests.log`, `work/promotions-ui-qa/report.json`, and `work/promotions-fixture/native-verification.json`. Earlier failing checkpoints are superseded by the final evidence. Private connection configuration must remain outside source, screenshots, and public evidence.

## Blocker and continuation

Andrew has selected the dedicated Ops account as the promotion tool's owner and operator. Its exact email belongs only in private configuration and any specific approval request. Do not substitute another signed-in identity because it is available or already authorized.

The previously staged private Apps Script project belongs to a different manager identity and is superseded. Leave that project, its configuration, and permissions untouched; do not reuse it for this setup. Its earlier editor acceptance and authorization-page reachability are historical staging evidence, not verification of the intended Ops-owned application. No consent was granted and no app promotion was written in that staging attempt.

The earlier proposed Editor grant on the synthetic workbook was rejected by automatic approval review and remained unapplied. Andrew has now withdrawn that sharing request. Do not retry it or broaden another account's permissions as a workaround.

Current account-correction checkpoint: sign-in is complete. The full authenticated Ops email was verified in Apps Script, and Overview confirms the authorized replacement project is owned by that account. The existing synthetic workbook remains solely Ops-owned. The replacement is named **GYM IN A BOX Promotions — OPS PRIVATE TEST**. Resume this project; do not create another or return to the superseded project. No immediate owner action or secret entry is requested.

The replacement project's saved `Code.gs` and `Index.html` match reviewed application `f00bea9b2bf225bce311d2f6b680088f31452bd8` through exact readback with normalized line endings. The saved manifest matches in text and meaning, including New York timezone, `MYSELF` access, and `USER_ACCESSING` execution. Private properties were verified against the exact Ops owner and existing synthetic workbook. No application source changed and no additional sharing grant was made.

The owner-only head development entry (`/dev`) is staged. **Review Permissions** was tried twice in total; the current hosted page says **Waiting for authorization**, and no matching consent popup is exposed in the in-app browser's tab inventory. The blocker is access to the actual consent page, not missing owner approval. No Google grant has actually been applied. An existing Edge authorization session belongs to a different account and must remain untouched. Preserve the current setup; do not start browser/tool repair or switch accounts. The application runtime has not run, and real promotion saves and independent persisted readback remain unverified. Native baseline visual QA passed as recorded above; native visual QA after hosted saves remains pending. No immediate action has been assigned to Andrew.

Before real TEST promotion writes, verify that the active/effective runtime identity and any final deployer still align with the verified Ops workbook owner, script owner, and private configured owner. Keep exact account, workbook, project, and deployment identifiers only in task-private configuration. Do not substitute identities or switch, close, or restart existing browser sessions.

Andrew explicitly approved Google consent for **GYM IN A BOX Promotions — OPS PRIVATE TEST** under the verified dedicated Ops account: primary-email access and permission to see, edit, create, and delete all Google Sheets accessible to that account, including live operational spreadsheets. Overview confirmed exactly `spreadsheets` and `userinfo.email`. Do not re-request this identical consent. Apply it only when the actual consent page visibly matches the approved account, app, and scopes. This approval authorizes neither additional sharing nor edits to live operational data. The application's exact-workbook guard constrains its behavior; it does not narrow Google's OAuth grant to the synthetic workbook.

Executable continuation prompt:

> Resume the existing replacement project, GYM IN A BOX Promotions — OPS PRIVATE TEST, from `docs/promotions-test-handoff.md` and private task configuration. Do not create another project or change the reviewed application `f00bea9b2bf225bce311d2f6b680088f31452bd8`. Andrew already approved this exact app under the dedicated Ops account for primary-email access and see/edit/create/delete access to all accessible Google Sheets, including live operational spreadsheets. Do not ask for identical consent again. The current development page says Waiting for authorization; after two Review Permissions attempts, no matching consent page is exposed and no grant has been applied. Preserve setup and leave the superseded project, withdrawn sharing request, and wrong-account Edge authorization session untouched. Do not start tool repair, substitute accounts, or switch, close, or restart existing browser sessions. When the actual consent page is available and visibly matches the approved account, app, and scopes, complete that already-approved consent. Pause for private user entry only if a password or verification code is actually required; never request or store it in chat. Then verify active/effective runtime identity against the private Ops configuration and finish the MYSELF/USER_ACCESSING TEST deployment. Verify owner access and rejection of unauthorized or blank identities. Browser-test stripe/degree, belt change, correction, duplicate names, unknown/missing ranks, double taps, concurrent/stale edits, and failed/lost responses against the existing synthetic workbook. Verify exact events, dates, before/after ranks, approver versus recorder, and history through an independent fresh session and native readback; complete after-save workbook and app visual QA. Preserve all six legacy tabs, both released kiosks, and the live promotion workbook. Return the protected TEST review entry point and final evidence. Keep PR #85 TEST only and unmerged; no additional sharing, live data edits, production release, or migration is authorized. No immediate action is assigned to Andrew while the matching consent page is unavailable.
