# Promotions TEST handoff

Status: implementation and simulated integration checks pass; **the hosted Google workflow is not verified or ready for owner review**. The separately frozen kiosk hotfix in PR #84 was explicitly authorized and merged as `1f16dd6ac2e5a1c974b50a98d14f31dbc2453b31`; both gyms have published it, with final release verification recorded in PR #84. Keep both released kiosks untouched. Promotions PR #85 remains draft, unmerged, separate, and TEST only.

Application: `f00bea9b2bf225bce311d2f6b680088f31452bd8` on `prototype/promotions-log-test-20260913`, based on main `93dba9e`. Core files are `promotions/Code.gs`, `promotions/Index.html`, and `promotions/appsscript.json`. No production deployment or live migration is authorized.

## Implemented

The protected TEST flow finds a student, previews one stripe/degree or a belt change, and records the promotion with its New York date, before/after rank, fictional approver, authenticated recorder, and permanent event/request identities. Missing students require explicit registration; unknown ranks require explicit confirmation; identical names retain separate identifying labels. Corrections append an audit entry and preserve the original.

`Promotion History` is the sole rank authority. `Students` is a derived view. A shared save lock, exact request fingerprints, and expected revisions prevent duplicate retries and stale concurrent awards. Lost responses retain the same request; recovery checks repair the derived view or report it pending. Late callbacks preserve another student's input and cannot restore a submitted draft or downgrade a confirmed rank.

The manifest specifies Google access `MYSELF` and execution `USER_ACCESSING`. Both the page and every callable data operation require nonblank active and effective Google identities matching the privately configured owner. This has automated coverage; real hosted enforcement remains unverified. Selecting an approver is explicitly not authentication or proof of approval. Production entry/approval policy remains an owner decision.

## Verified evidence

- Final automated run: **848 passed, zero failures/skips**, including **32 promotions tests**. This also covers the retained M1 baseline. Command: `npm test`. After that run, Google rejected a numeric separator in the server lock timeout; the equivalent literal was corrected, all 32 promotions tests passed again, and the actual Google editor accepted the saved backend. The interface is unchanged from the browser-tested revision.
- Actual Chromium with simulated `google.script.run`: **16 final-source workflow groups passed**, including duplicate submissions, missing/unknown students, failure recovery, late replies, and corrected callback ordering. Phone 390px, tablet 820px, and laptop 1440px layouts were visually inspected; suggestions stay bounded and controls are at least 48px. The unchanged request timeout was exercised for approximately 30 seconds. These checks do not prove Google authentication or durable backend writes.
- The separate private synthetic workbook contains **8 baseline student/history records** and **6 preserved legacy tabs**. Synthetic blanks, unknown dates, annotations, duplicate names, and black-belt degrees are retained. Local fixture renders and native data readback were checked. **Native workbook visual sanity has not been confirmed.** No actual promotion has yet been verified through the hosted application and an independent persisted readback.

Task-private evidence remains outside the repository: `work/promotions-final-tests.log`, `work/promotions-ui-qa/report.json`, and `work/promotions-fixture/native-verification.json`. Earlier failing checkpoints are superseded by the final evidence. Private connection configuration must remain outside source, screenshots, and public evidence.

## Blocker and continuation

Andrew has selected the dedicated Ops account as the promotion tool's owner and operator. Its exact email belongs only in private configuration and any specific approval request. Do not substitute another signed-in identity because it is available or already authorized.

The previously staged private Apps Script project belongs to a different manager identity and is superseded. Leave that project, its configuration, and permissions untouched; do not reuse it for this setup. Its earlier editor acceptance and authorization-page reachability are historical staging evidence, not verification of the intended Ops-owned application. No consent was granted and no app promotion was written in that staging attempt.

The earlier proposed Editor grant on the synthetic workbook was rejected by automatic approval review and remained unapplied. Andrew has now withdrawn that sharing request. Do not retry it or broaden another account's permissions as a workaround.

Current account-correction checkpoint: the connector freshly confirms the dedicated Ops identity and sole ownership of the existing synthetic workbook. Two bounded native-script metadata searches found no suitable owned script, though container-bound projects may not appear there. The existing Apps Script CLI is not logged in. Browser policy blocked profile-settings navigation; no workaround or existing-session change was attempted. An isolated Codex browser is at the intended Ops account's private Google password step. The replacement project has not been created and its owner/deployer/runtime identity is not yet verified. Finish the user's private sign-in first, then verify the full authenticated email before creating a replacement or reaching consent. Do not request, read, or store passwords or verification codes.

Before any new TEST integration writes, verify that the actual synthetic workbook owner, Apps Script owner, deployer, configured owner, and accessing/effective runtime identities all align with the dedicated Ops account. A connector's access or a browser's visible account is not proof of that alignment. Keep exact account, workbook, project, and deployment identifiers only in task-private configuration. Do not switch, close, or restart existing browser sessions to make the identities match.

No new sharing grant or Google consent is authorized without specific approval for its account, target, and permissions. The app requests `spreadsheets` (read, edit, create, and delete Google Sheets accessible to the consenting account) and `userinfo.email` (read its primary email). Consent can therefore cover other Ops-accessible Sheets, including live Sheets. The application's exact-workbook guard constrains its behavior; it does not narrow Google's OAuth grant to the synthetic workbook. Present that scope accurately before any required approval.

Executable continuation prompt:

> Continue from `docs/promotions-test-handoff.md` and application `f00bea9b2bf225bce311d2f6b680088f31452bd8`, preserving the existing promotion implementation. Use only the dedicated Ops owner/operator identified in private configuration. Leave the superseded manager-owned project untouched. Never substitute identities or switch, close, or restart existing browser sessions. The old sharing request is withdrawn. Verify that the synthetic workbook, script ownership, deployer, configured owner, and active/effective runtime identity all align with the dedicated Ops account. Obtain specific approval before any new sharing grant or OAuth consent, accurately explaining that the Sheets scope may include other Ops-accessible and live Sheets. Then finish the owner-only Google Apps Script TEST deployment with MYSELF access and USER_ACCESSING execution. Verify the intended owner succeeds and unauthorized or blank identities cannot read or write. Browser-test stripe/degree, belt change, audited correction, duplicate names, unknown/missing ranks, double taps, concurrent/stale edits, and failed/lost responses against the real synthetic workbook. Confirm dates, approver versus recorder, exact event IDs/counts, and before/after history through an independent fresh session and native sheet readback. Visually inspect the native workbook and phone/tablet/laptop app. Preserve all six legacy tabs and keep both released kiosks and the live promotion workbook unchanged. Return the working protected TEST review entry point and update this handoff with exact deployment and final evidence. Keep PR #85 TEST only and unmerged; do not release production or migrate live students without separate approval.
