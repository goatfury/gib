# Promotions handoff: pre-merge import checkpoint

**PRE-MERGE CHECKPOINT — 2026-09-13, 7:06 PM America/New_York.** The complete approved baseline import passed independent native verification and an actual Google runtime read. The Guide completion write/readback and visible instructions passed; the exact reviewed Version 5 editor source is restored. All pre-merge checks passed, with both activation gates still off at this checkpoint. No production publication is claimed here. This document is committed before the authorized merge; the PR #85 description is the authoritative later activation and published-verification receipt: https://github.com/goatfury/gib/pull/85.

Andrew authorized replacing the seven-stage procedure with ordinary native range writes whose full arguments remain below 25,000 bytes, each separately reviewed. All 41 data actions were accepted by ordinary review and verified individually; each full argument was below 22,500 bytes. The complete Students and History baseline now matches the approved private manifest, including native values/types and unique permanent identities. No tab was recreated and no identity was regenerated. Final native comparison confirmed all six original tabs' cells, styles, dates, notes, validation, and metadata unchanged; only their approved tab-index shift of three occurred. Workbook ownership and sharing are unchanged.

Historically, automatic approval review rejected the unexecuted 844,723-byte atomic import against its 200,000-byte limit and expressly prohibited bypass. Under the subsequently approved seven-stage procedure, Stage 1 created the three tabs and headers. Stage 2's 142,408-byte raw request was rejected before execution because its platform review representation exceeded the limit; stages 3–7 were not attempted. The later, explicitly authorized small native writes resumed from that verified existing checkpoint through ordinary review. Those earlier rejections remain historical evidence, not the current import status. Do not encode, indirectly execute, or otherwise reroute a rejected request to evade review.

Verified nonproduction review route at this checkpoint: **https://deploy-preview-85--gib-live.netlify.app/m1/**. Its immutable artifact is https://6aa718f831cf6b089f1955a9--gib-live.netlify.app/m1/; protected review uses the authorized stable route. The TEST artifact passed all 68 served-file hash checks and 22 excluded-path checks. Full frozen application revision: **`d9ee18cad2161446769f59a68b06f4d667af8365`**. Production publication follows this pre-merge document and must be verified separately.

At this pre-merge checkpoint, work is on `prototype/promotions-log-test-20260913`, main is at `1f16dd6`, PR #85 is unmerged, and both live deployment identities are unchanged. The kiosk hotfix in PR #84 was separately authorized, merged, and published at both gyms; preserve those released kiosk behaviors. The granted launch scope is recorded at the end of this document. Richmond publication is outside that scope.

## Pre-merge rollout state and Sign-In protection

The current application fixes an optional-resource loading problem: promotion configuration and CSS now load dynamically after the document is ready, so they cannot hold up the existing Sign-In form. This changes neither the interface nor promotion backend logic. Required continuous-integration checks passed, together with **64 focused tests, including eight new regressions**. In the actual browser, the configuration response was held and then the CSS response was held separately; name, notes, and class controls remained responsive before those files were released. No records were submitted for these checks. Monday schedule readiness checks passed, and existing Sign-In behavior remains unchanged.

Seven private Google LIVE properties have been staged, with `LIVE_ENABLED=false` saved and prior properties preserved. Five new Netlify LIVE keys were created: the activation flag is confirmed false in the production build and function scopes, and the other four settings are staged in production function scope. The endpoint and bridge key remain marked secret. Existing TEST and production credentials were retained. These configuration preparations do not mean the promotion feature is live. A private title property's trailing whitespace had been trimmed by the editor; a temporary owner-editor helper set that property to the workbook's exact name without editing the workbook. The exact Version 5 backend source was then restored. This required no redeployment, new OAuth grant, or account change. Fresh settings readback confirmed both the exact original title and all previous TEST properties. Do not resave that title through the trimming editor when later activating the bridge. Private property values and workbook identity remain outside this document.

The patched artifact also passed a fresh protected lookup in the existing Codex review tab. Jordan Evening remained White Belt four stripes; history retained the typed TEST Coach Pilot entry and earlier ID-based attribution with distinct recorder provenance. No new promotion was submitted. The tab was returned to clean Sign-In with no console warnings or errors.

The approved integration reuses Revolution's existing authorized-device cookie and server credential. It requires no instructor Google login, new account, repeated OAuth consent, new physical setup, cookie reset, or tablet reconfiguration. LIVE requests are restricted to Revolution at exactly **https://gib-live.netlify.app**, with both Netlify environment gates and private Google properties required. TEST remains a separate destination, and the Richmond kiosk and publication path remain unchanged. Both Google and Netlify activation gates are confirmed disabled.

The live workbook has an existing owner other than the dedicated Ops account. Ops already has Writer access; no new access grant is needed. Do not describe Ops as the live workbook owner. The account running the backend and a freely entered awarder name remain separate identities.

A read-only live-workbook dry run accounted for active and Former source records. The approved import preserved separate student identities without name merges, known ranks, and explicit unconfirmed ranks. Duplicate-name records remain distinguishable and require individual handling. Former rows remain in their original source. The Guide, `Students` with 12 columns, and `Promotion History` with 27 columns were added once; Students and baseline `REGISTER` records now match the complete approved manifest. Final verification confirmed all six original tabs' native content unchanged, with only the approved index shift. Exact live source counts, identity bindings, and duplicate groups remain in the private validated import manifest.

Unknown source promotion dates remain blank. The imported records are explicit **LEGACY BASELINE IMPORT** records linked to the private import manifest, not synthetic records or invented promotion awards. Native verification confirmed the complete approved values, types, identities, and blank award dates. The original source-fingerprint and absent-destination guards applied before Stage 1; subsequent writes verified and preserved that existing checkpoint. Do not rerun tab creation, delete and recreate the imported tabs, regenerate identities, or restart an import blindly. The Guide completion write received ordinary approval, its native readback matched, and its completed instructions were visibly confirmed. Both gates remain off at this pre-merge checkpoint.

The existing Google deployment remains **Version 5**; its endpoint, executing account, and previously approved access settings are unchanged. An actual read-only execution through the existing owner editor passed at 23:05 UTC: effective account, exact configured workbook identity/title, New York timezone, complete baseline identities and event/request uniqueness, known/unconfirmed ranks, blank award dates, and Students/History projection agreement all verified. The execution wrote no cells or properties and completed in about three seconds. This successful read does not establish that intermittent latency is fixed or constitute physical-tablet proof. The temporary verifier did not change the deployed version; the exact reviewed editor source was restored and its clipboard hash verified at 23:06 UTC, without redeployment. No new Google account, OAuth consent, sharing, or project was created. Prior actual TEST save/readback evidence remains applicable because the current loading fix does not change backend logic.

One observed Google execution, timestamped **2:20:35 PM**, took **103.498 seconds** and had status **Completed**. This locates at least one delay inside Google execution; the exact operation remains unknown, and Completed alone does not establish a committed promotion. The server's 25-second and browser's 30-second timeouts remain unchanged. No narrow correctness fix is supported by the available evidence; the pilot retains this latency limitation. Do not claim the connection is fixed. There is no automatic resend. An uncertain save keeps its exact request for deliberate reconciliation, without claiming Not saved or creating another transaction. TEST-only failure headers report a bounded upstream stage and elapsed time without private content; live responses do not expose those diagnostics.

## Prior release-candidate acceptance on `45d899bf`

These actual save/readback results remain backend evidence for `d9ee18c`, whose change only removes optional resources from Sign-In's startup path. They do not establish completion of the staged live import or a production release.

- Required continuous-integration checks passed **1,016 of 1,016 tests**. The corrected isolated preview used deploy-preview context; **68 of 68 served-file hashes matched**, **22 excluded paths returned 404**, and both live deployment identities remained unchanged.
- In the actual visible panel, one TEST Jordan Evening promotion saved **White Belt three to four stripes**, attributed to the typed **TEST Coach Pilot**. It succeeded without Check save or Retry. A fresh document showed four stripes, retained earlier ID-based Avery attribution, and displayed separate **Authorized TEST tablet** provenance. The next editor started blank.
- Independent native verification confirmed **31 History events and 10 Students**, with exactly one new event. All prior 30 events and all six synthetic legacy tabs remained exact. Students projections, permanent identities, and dates passed verification.
- Phone, tablet, and laptop views at **390, 820, and 1440px** passed visual checks. Tablet content and scroll widths both measured 805px; laptop widths both measured 1425px. Suggestions did not automatically replace typed text, and display checks created no extra promotion.
- An unsaved **Ave** entry, its visible suggestion, and selected history cleared automatically to clean Sign-In after inactivity. A fresh editor was blank. A synthetic unsent Sign-In name and notes disabled the log entry while preserving the draft. Only that test draft was cleared; no Sign-In or payroll records were created. Independent review also confirmed the existing next-person behavior's source was unchanged, so earlier deadline checks remain applicable.
- Four bounded reads succeeded in **3.321, 8.995, 2.356, and 3.740 seconds**. These successful samples do not establish that intermittent Google latency is fixed.
- The final fresh document had **no console warnings or errors**. The actual visible review tab was left at clean Sign-In at the top with its normal viewport restored. No physical tablet, physical sleep/resume, or hardware/OS keyboard pass is claimed.

If the approved release later needs rollback, disable promotion activation and restore the prior application code while preserving all promotion records, including entries made after launch. Do not restore an old workbook snapshot, delete later history, reset device cookies, or discard pending requests. Preserve the existing Sign-In and Staff Clock behavior and data. At this pre-merge checkpoint, production code has not been deployed but the approved baseline import has executed. Keep the verified imported records intact; no cleanup or workbook restoration has been authorized.

## Current instructor workflow and trust model

The requested model follows the paper promotion book: a trusted instructor uses an authorized gym tablet to look up a student or record an awarded promotion. **Promoted by is freely typed attribution for whoever awarded the promotion, who may be different from the tablet operator.** It is not authenticated identity. There is no individual Google login, instructor password, digital-signature claim, or separate approval step for each promotion. This trust model is the chosen direction; it is not an undecided entry policy.

**Instructor Sign-In remains the primary screen.** The log has one small, secondary entry below the existing Sign In controls, inside the form card and before Staff Clock. The log panel stays closed until deliberately opened. The previous pair of large top-level Sign-In/Log buttons was an implementation error that Andrew rejected; do not restore that strip or give promotions equal prominence above the form. Tablet authorization is required before lookup or recording. The named awarder is distinct from the technical identity reaching Google Sheets: the backend operates under the dedicated Ops account, while integrated history records authorized-device provenance. The retained `approver_id` and `approver_label` fields preserve historical attribution; they do not introduce an approval workflow. Available fictional TEST instructor names are optional suggestions, not a mandatory or verified production roster. LIVE suggestions derive only from names already in its own history.

The flow finds a student, shows recorded rank and history, previews one stripe/degree or a belt change, and records the New York date, before/after rank, entered awarder name, recorder provenance, and permanent event/request identities. Missing students use explicit registration; unknown ranks use individual current-rank confirmation; identical names retain distinguishing labels. Corrections append an audit entry and preserve the original. An unclear record does not require a whole-book history audit before other students can be handled.

Lookup and form details clear after **60 seconds without activity**. A confirmed save returns to Sign-In after **3 seconds**; later touches do not extend that success display. Clear/Back returns immediately. An uncertain save retains its exact pending request separately for Check save or Retry this entry. Clearing the visible lookup must not discard that unresolved transaction, create a new request, or allow a late response to reopen private details. Suspended timers are checked when the page resumes.

## Candidate implementation and workbook views

The integration spans `m1/index.html`, the `m1/promotions-*` client/template/style/lifecycle files, configuration/setup, the promotions server route/runtime, and the signed tablet entry path in `promotions/Code.gs`. The older `promotions/Index.html` owner page remains TEST reference material. Current source is **`d9ee18cad2161446769f59a68b06f4d667af8365`**, adding the optional-resource loading fix to `45d899bf` while retaining its guarded LIVE support, typed interface, and corrected navigation: no top strip, one small entry below the form, and in-log Clear/back. The verified typed-attribution application was `cf8a03530f406a4d3c98ffe376699fc46eecbb9a`; its hosted evidence below is a prior milestone. Existing authorization, history, and request recovery remain intact.

Lookup and writes use the configured Revolution server route and must match the authorized destination. The candidate validates the authorized device, exact installation/origin, and signed server-to-Google request. Every new LIVE tablet write, including registration, requires a nonblank typed attribution name. TEST preserves legacy ID-based exact retries; historical IDs and names remain readable in both modes. Lookup does not require attribution. An endpoint URL or typed instructor name alone must not grant access. Connection values and credentials stay in private configuration, outside public source, screenshots, and handoff evidence.

## Prior milestone: freely typed attribution update

Application `cf8a03530f406a4d3c98ffe376699fc46eecbb9a` replaces the mandatory roster dropdown with one text field: **Promoted by**, placeholder **Enter instructor’s name**. Up to four matching existing names appear in a small list below the field after typing; none is preselected. The list is bounded to 180px, supports explicit pointer/keyboard selection, and closes on Escape, blur, selection, or privacy clearing. An unlisted name requires no roster addition. The existing confirmation shows the entered name. Outer whitespace is trimmed; spelling, Unicode, accents, apostrophes, hyphens, case, and internal spacing are preserved.

New requests use `approverName`, stored in existing history `approver_label` with a blank `approver_id`. Existing IDs and names remain untouched; legacy pending requests still use their original `approverId` and fingerprint. Ambiguous requests containing both fields are rejected. No history schema migration, roster-management flow, or authorization change was introduced. New interactions start blank; unresolved saves preserve the exact original request separately from the cleared visible form.

At this milestone, all **969 automated tests passed**, including 21 new attribution regressions. The existing private Google TEST deployment was updated to **Version 4**, using the same dedicated Ops owner, endpoint, and previously approved access settings. No new account, consent, sharing, or project was needed. Actual hosted saves and independent native readback verified **10 Students and exactly 30 History events**; all prior 28 events remained unchanged. These counts and hosted checks belong to that source, not the later release-candidate acceptance.

`Promotion History` remains the rank authority. `Students` is its current derived view, updated on confirmed writes or reconciliation; workbook views must not silently disagree. Shared save locking, exact request fingerprints, and expected revisions protect retries and concurrent entries. Unknown confirmations, duplicate identities, zero stripes, degrees, and corrections retain their earlier behavior.

TEST work preserved all six legacy tabs as source history. The completed live baseline import likewise preserves the original source blocks and adds a separate current Students view. These baseline records do not make every earlier source award a complete imported event history. The preparation utility and tests in `docs/promotions-legacy-mapping.md` perform no migration or workbook writes; the later authorized native writes populated the new views. Earlier rejected import procedures and their verified continuation are recorded above.

## Approved TEST deployment

Andrew's explicit “sure” approved the bounded Google TEST deployment-access change for Version 3. **Version 5 is now the saved and source-verified TEST deployment**, executing as the same dedicated Ops account with access still set to Anyone and application-level signed authorization enforced for the tablet route. This enables server requests without an interactive Google login; the URL alone does not authorize lookup or recording. No new OAuth grant, workbook sharing, account substitution, or project was created. Actual release-candidate save and fresh readback passed.

Earlier OAuth consent remains complete for the dedicated Ops project: primary-email access plus see/edit/create/delete access to Sheets accessible to that account, including live operational spreadsheets. Do not request identical OAuth consent again. Consent and TEST deployment approval alone did not authorize live edits; the later, separate Revolution launch approval is recorded below. Preserve the superseded wrong-account project and existing sessions.

The Revolution TEST Netlify configuration contains the nine required private keys scoped to TEST functions. The current isolated release candidate passed artifact preflight and hosted acceptance. Private configuration values, Google identifiers, and backend URLs remain outside this document and public artifacts.

## Prior milestone: freely typed attribution verification

- Frozen application **`cf8a03530f406a4d3c98ffe376699fc46eecbb9a`** passed **969 tests**, including 21 new attribution regressions. That deployed artifact matched **68 of 68 served-file hashes**, and all **22 excluded paths returned 404**. Both live kiosks and the live promotion workbook remained unchanged.
- In the actual user-visible preview, choosing the visible **TEST Coach Avery** suggestion saved TEST Alex from **Blue Belt one to two stripes**. The next interaction started with a blank Promoted by field.
- Freely typing the unlisted **TEST Renée O'Neill-Santos** saved TEST Taylor from **Black Belt seven to eight degrees**. The initial response timed out. Clicking **Check save once** recovered the original event exactly once; no Retry or new save was submitted.
- A fresh browser reload and lookup showed Taylor at Black Belt eight degrees. Expanded history displayed the exact Renée accent, apostrophe, and hyphen alongside earlier ID-based Blake and Avery attribution, with separate **Authorized TEST tablet** and **Earlier log** recorder provenance.
- A fresh Alex lookup showed Blue Belt two stripes. Expanded history retained the new typed Avery attribution, earlier ID-based Avery/Blake entries, and both audited corrections. Opening a new editor after these reads showed a blank Promoted by field, with no previous name carried forward.
- Independent native verification confirmed **exactly 30 History events and 10 Students**. Both new entries have blank attribution IDs and their exact entered names, with the same authenticated recorder kept separate. All prior **28 event IDs, names, and rows**, the six legacy tabs, roster, and private ownership were unchanged.
- The final explicit suggestion list was visibly inspected at **390 × 844, 820 × 1180, and 1440 × 900**. Tablet and laptop content widths matched their scroll widths, with no overflow. While the Avery suggestion was visible, both the typed field and confirmation remained the literal **Ave**, proving there was no automatic selection.
- On the final artifact, an unsaved **Ave** entry with the visible Avery suggestion and selected Alex/history cleared automatically while left idle. The subsequent view showed clean Sign-In with all promotion details, typed text, and suggestions removed. No third promotion was submitted during these final display/reset checks.
- The final fresh browser document had **no console errors or warnings**. The actual review tab was left at clean Sign-In at the top in its normal viewport.
- **Intermittent TEST upstream timeouts remain; their root cause is unestablished.** A successful later request and successful Check save recovery do not establish that the connection is fixed. An uncertain response keeps the exact pending intent and does not falsely report Not saved.

## Prior milestone: integrated workflow and navigation verification

The following results belong to the earlier integration and corrected-navigation milestones. Their source revisions, test counts, and workbook totals are historical checkpoints, followed by the typed-attribution milestone above.

- **`npm test`: 948 passed, zero failures, zero skipped**, on navigation revision `155d575`; the following `8331326` revision changed only EOF whitespace. This included the retained kiosk checks and integrated promotion lifecycle, mounted-client, authorization, transport, and history regressions.
- The corrected navigation was checked in the actual user-visible Codex panel on `8331326`: Sign-In appears directly below the original header, the log entry is a small transparent 14px control below Sign In, and the log stays closed until opened. The entry opened the real protected lookup as **Authorized TEST tablet**; Clear & return restored the sign-in form. Narrow-panel and 1440px laptop views had no horizontal overflow or console errors/warnings. The active Sign-In draft barrier was also exercised on `155d575`. No additional promotion or payroll writes were made for this layout correction. Both final preview URLs matched all 68 artifact hashes; all 22 excluded paths returned 404, and both live deployment identities remained unchanged.
- At the first integrated native checkpoint, hosted kiosk controls had recorded **four integrated events**: a stripe, its audited correction, a black-belt degree, and a belt transition. Native workbook verification showed **9 Students and 24 History events**. Two authorized-device recorder identities remained separate from the fictional selected instructors. A duplicate click created only one event. The completed final checkpoint below includes the later hosted actions.
- During the degree save, an actual page reload returned to clean Sign-In while preserving the unresolved request. **Check save reconciled the original event once.** A later pending belt request also reconciled once through Check save. Intermittent Google transport timeouts left an unconfirmed intent; the interface did not falsely report Not saved.
- The prior **20 History events and all six legacy tabs remained exact** at the native checkpoint. The four new events and current Students view were checked against the expected ranks and audit fields.
- The final deployment matched **68 of 68 artifact hashes**. All **22 excluded private/source paths returned 404**. Both live kiosks' **56 checked assets and deployment identities remained unchanged** during this TEST integration.
- Actual concurrent-browser QA saved TEST Ellis from Brown Belt two to three stripes with Avery selected in Edge. Another browser's older Brown Belt two-stripe draft, with Blake selected, was rejected as changed in another session. Refresh showed Brown Belt three stripes.
- Both labeled TEST Jordan candidates were visible. The Morning student's White Belt one-to-two stripe save succeeded with Avery selected; a fresh Evening lookup still showed White Belt three stripes, unchanged.
- Explicit registration created **TEST Integration Unknown / Integrated fixture** with Blake selected and an unknown rank. On the final artifact, verified-baseline confirmation saved White Belt zero stripes with Blake selected. A fresh browser document read White Belt zero and **Date not recorded** for the latest promotion; expanded history showed both registration and rank confirmation with Blake and **Authorized TEST tablet**. The baseline confirmation did not invent a promotion award or date. Registration guidance explains the separate student identity, and current-rank guidance distinguishes a verified baseline from a new award.
- An actual unknown-student lookup was cleared before its response arrived. After more than 30 seconds, the page still showed clean Sign-In with no old student details restored.
- Final hosted viewport screenshots were inspected at **390 × 844, 820 × 1180, 1180 × 820, and 1440 × 900**. Page content fit the viewport at each size (`clientWidth` equaled `scrollWidth`); normal viewport images confirmed the interface was clear. A full-page screenshot stitching artifact was distinguished from the actual page. The final fresh document had **no console errors or warnings**, and Arrow Down then Enter selected a search result.
- Actual hosted typing at approximately 33 seconds renewed lookup activity. The query remained visible at approximately 72 seconds from the start, about 38 seconds after typing, then returned to clean Sign-In when checked about 70 seconds after typing. The **3-second confirmed-save return** was also observed in the hosted UI. These are actual elapsed-time observations in addition to the exact-deadline automated tests.
- Actual hosted navigation respected an active Sign-In draft. The promotion checks created no Sign-In or payroll records. Earlier mocked browser checks supplement these final hosted results.
- **Independent final native verification passed: 10 Students and 28 History events**, including eight integrated entries recorded by two authorized-device identities with separate selected-instructor attribution. All prior 24 events and all six legacy tabs retained exact native content. Ellis was Brown Belt three stripes, Jordan Morning White Belt two, Jordan Evening White Belt three, and the newly registered unknown student was confirmed at White Belt zero. Every Students view matched authoritative History; correction links, unique event/request identities, and New York dates passed verification. Native Students/History visual checks passed, and the original legacy visual evidence remained valid with exact native preservation. The TEST workbook remained private and solely Ops-owned.

## Prior review delivery and continuing physical release boundary

The original integrated workflow and navigation checks passed. The prior typed-attribution artifact passed actual saves, exact-request recovery, independent native verification, fresh history readback, visible suggestion checks, and next-person clearing at **https://deploy-preview-85--gib-live.netlify.app/m1/**. That artifact opened directly on the normal sign-in form, with the small log entry below the form. The actual Codex panel is the review surface Andrew confirmed he could see; access and delivery do not imply release approval. Current release-candidate delivery passed as recorded above, while the intermittent Google latency limitation remains open.

No physical tablet, physical sleep/resume cycle, or hardware/OS keyboard was tested. Automated suspended-page checks and browser viewport/keyboard checks do not replace those physical checks. The permanent release gate in `docs/m1-release-checklist.md` remains in force for a later production release; these physical release checks do not block review of the isolated TEST preview.

At the prior milestone, the working review route was the actual Codex browser panel opened at the URL above. Its protected log loaded successfully, then returned to clean Sign-In. **Andrew explicitly confirmed the actual panel was visible: “yes, I see it.”** This confirmed that milestone's access and delivery; it did not approve the previous top-level navigation design. The current integration continues to use existing device authorization, without a new Google sign-in or access grant.

An automation-created tab alone did not establish delivery: Andrew initially saw the spreadsheet. Open the review URL through Codex's panel control, inspect the actual panel tab, verify its protected access, and leave that tab selected. Do not replace this confirmation with a screenshot from a separate automation tab. Documentation pushes can regenerate the default preview without the TEST feature flag; preserve the verified application artifact when updating this handoff.

## Historical evidence: Google-owner reference only

These results belong to the previous owner-authenticated reference workflow. They do not prove new tablet authorization, instructor entry, inactivity behavior, server connection, or hosted kiosk integration.

- Earlier complete automated run: **848 passed, zero failures/skips**, including **32 promotions tests** and the retained M1 baseline. A later lock-timeout literal correction passed the same 32 promotions tests. Reference revision `9c73042f944dd9b4e6207c837d95ed7d53ddd1a8` passed **34 promotions tests**, including native-literal and uncertain-save regressions. The older complete-suite result is not a run on the current integrated candidate.
- Actual Chromium with simulated `google.script.run`: **16 reference-source workflow groups passed**, including duplicates, missing/unknown students, failure recovery, late replies, and callback ordering. Phone 390px, tablet 820px, and laptop 1440px layouts were inspected; suggestions were bounded and controls at least 48px. The approximately 30-second request timeout was exercised. These were simulated Google transport checks.
- Native baseline visual sanity passed across the private workbook's eight tabs, initially containing **8 baseline student/history records** and **6 preserved legacy tabs**, including duplicates, unknown rank, blanks, `?`, `Transplant`, and `Early 2014`. Subsequent native Students/History inspection showed nine students and twelve added events with expected dates, ranks, and references.
- Actual version-2 reference-app layout checks passed at 1440px, 820px, and 390px. Fresh documents showed the corrected rank and unknown-registration/zero-stripe case with history intact. Console errors were empty. These were hosted owner-page checks, not integrated kiosk checks.
- The real owner session's recorded identity was independently verified as the dedicated Ops account. A fresh anonymous request redirected to Google login without reference-app data. Nonowner/blank-identity guards had automated coverage; a signed-in wrong-account browser test was not performed. The old anonymous redirect is not the acceptance criterion for the authorized-tablet route.

Private evidence includes `work/promotions-final-tests.log`, `work/promotions-ui-qa/report.json`, the private fixture baseline, repair and recovery records, and `work/promotions-fixture/integrated-final-native-verification-private.json`. Exact accounts, workbook/project/deployment identifiers, backend URLs and hashes, and connection configuration stay outside the repository and public evidence.

## Historical save recovery and native checkpoint

The first owner-reference TEST Alex stripe appended once, but native Sheets consumed an initial apostrophe in its copied legacy reference. Strict readback failed and the old response incorrectly said Not saved. The fix preserved native literal escaping and classified post-append/readback failures as uncertain confirmation, without weakening historical validation. Only the damaged TEST `Promotion History!Y10` reference was restored through native string-value entry; the promotion identity, request, rank, and date were not replaced. Retrying the **same original UI request** recovered that event and rebuilt Students. This is real post-commit failure/retry evidence; an intentional network-drop test was not performed.

Completed owner-reference transactions:

- TEST Alex: one-to-two stripes, then an audited correction back to one, preserving both events.
- TEST Taylor: Black Belt five-to-six degrees without an ordinary stripe limit.
- TEST Rowan: Purple Belt four-to-five stripes, then Brown Belt zero, retaining earlier history.
- Duplicate-name TEST Jordan records: Morning White Belt zero-to-one; Evening White Belt two-to-three, as separate students.
- TEST Casey: unknown-rank confirmation to White Belt two stripes, then promotion to three.
- TEST Quinn: unknown-rank registration, rejected duplicate name/label registration, then White Belt zero confirmation.
- TEST Ellis: one-to-two saved; another tab's stale revision-one submission was rejected and Refresh showed two.
- TEST Morgan: archived record stayed read-only, with history available.

Independent native verification passed at **9 Students and 20 History events: 8 baseline plus 12 hosted events**. Final ranks, statuses, revisions, last-event references, and sequences matched. Student/event/request identities were unique; rejected duplicate/stale attempts and recovery created no extras. Correction link and reason matched. Every hosted reference entry had the verified Ops recorder, New York date `2026-09-13`, valid UTC timestamp/fingerprint, and separate fictional instructor attribution.

Original History rows retained native values, types, and formats. The repaired stripe and correction matched captured checkpoints. All six legacy grids, types, formulas, formats, properties, and original source references stayed unchanged. Students agreed with History. Original Students rank flags became text booleans under their imported format; authoritative history retained native booleans. That difference was documented. The workbook stayed solely Ops-owned, unshared, and on New York time. This is the retained pre-integration checkpoint, not proof of a new integrated write.

## Historical delivery and release boundary

Andrew confirmed the old owner's Add one stripe form was visible in Codex's right panel. An earlier external-browser error exposed no account identity, so its cause was not established. Delivery was verified in the Ops-signed-in Codex browser for the reference only. The new instructor review target must be the authorized TEST kiosk preview and must not depend on that Google owner session.

Browser viewport checks are not a physical-device pass. The approved TEST integration and navigation correction were deployed and verified in Andrew's actual review panel. PR #85 was unmerged at this document's pre-merge checkpoint. Those historical TEST approvals alone did not authorize live changes; the later Revolution launch approval below supplies the bounded scope. Preserve both released kiosk behaviors and all verified imported records through activation, publication, and any later rollback.

## Granted launch approval and remaining release steps

Andrew approved the following bounded Revolution launch scope, subsequently the seven-stage procedure, and then its replacement with ordinary, separately reviewed native writes below 25,000 full-argument bytes. Those approvals remain in force; do not request them again:

- Add the three tabs to the existing live workbook and import the separate baseline records from the private validated manifest, preserving all six original tabs. The complete baseline, actual Google runtime read, Guide completion, and exact editor-source restoration have passed verification.
- Configure and enable the private LIVE backend properties and Netlify flags for the existing Revolution integration, using its existing account access and device authorization.
- Merge PR #85, which triggers the automatic Revolution production release.

All pre-merge checks have passed. The remaining authorized steps are activation and PR #85 merge, followed by actual production verification. Preserve the existing production deployments as code rollback points. The candidate's production build is prepared locally at this checkpoint; it uses a fixed 36-file public allowlist, with private inputs, Google source, tests, and documentation excluded from static hosting. After automatic publication, verify the actual production artifact against the prepared source and installation profile and record the result in the PR #85 description. This pre-merge document alone is not a post-deployment receipt.

This scope includes **no Richmond publication, new account or Google login, additional sharing/OAuth, physical tablet setup or reconfiguration, cookie reset, pending-request discard, or replacement of historical or post-launch promotion records**. At this pre-merge checkpoint, private configuration is staged with both activation gates false, the complete baseline is natively verified, and no merge or production deployment has occurred. The six original tabs' native content, workbook ownership/sharing, and both published deployment identities remain unchanged. Earlier read-only schedule checks succeeded without warnings or source-hash changes. Detailed receipts, exact source counts, and the native execution ledger remain private.
