# Promotions TEST handoff

Status: **READY FOR PROTECTED TEST REVIEW — real hosted workflows, independent persistence checks, and native visual QA passed; no required TEST QA remains**. The separately frozen kiosk hotfix in PR #84 was explicitly authorized and merged as `1f16dd6ac2e5a1c974b50a98d14f31dbc2453b31`; both gyms have published it, with final release verification recorded in PR #84. Keep both released kiosks untouched. Promotions PR #85 remains draft, unmerged, separate, and TEST only.

Application: `9c73042f944dd9b4e6207c837d95ed7d53ddd1a8` on `prototype/promotions-log-test-20260913`, based on main `93dba9e`. Core files are `promotions/Code.gs`, `promotions/Index.html`, and `promotions/appsscript.json`. This revision fixes native Sheets literal escaping and uncertain save reporting, with two added regressions. The interface, manifest, authentication, and strict historical validator remain unchanged. No production deployment or live migration is authorized.

## Implemented

The protected TEST flow finds a student, previews one stripe/degree or a belt change, and records the promotion with its New York date, before/after rank, fictional approver, authenticated recorder, and permanent event/request identities. Missing students require explicit registration; unknown ranks require explicit confirmation; identical names retain separate identifying labels. Corrections append an audit entry and preserve the original.

`Promotion History` is the sole rank authority. `Students` is a derived view. A shared save lock, exact request fingerprints, and expected revisions prevent duplicate retries and stale concurrent awards. Lost responses retain the same request; recovery checks repair the derived view or report it pending. Late callbacks preserve another student's input and cannot restore a submitted draft or downgrade a confirmed rank.

The manifest specifies Google access `MYSELF` and execution `USER_ACCESSING`. Both the page and every callable data operation require nonblank active and effective Google identities matching the privately configured owner. The real hosted owner session has run, and its recorded identity was independently verified as the dedicated Ops account. A fresh anonymous request without cookies redirected to Google login without returning the app or synthetic data. Nonowner/blank-identity guards also passed automated coverage; a signed-in wrong-account browser test was not performed. Selecting an approver is explicitly not authentication or proof of approval. Production entry/approval policy remains undecided.

## Verified evidence

- Earlier complete automated run: **848 passed, zero failures/skips**, including **32 promotions tests**, covering the retained M1 baseline. Command: `npm test`. The subsequent equivalent lock-timeout literal correction passed all 32 promotions tests. Current application `9c73042` passed **34 promotions tests**, including two new native-literal and uncertain-save regressions. Do not relabel the older complete-suite run as a complete-suite run on the latest source.
- Actual Chromium with simulated `google.script.run`: **16 final-source workflow groups passed**, including duplicate submissions, missing/unknown students, failure recovery, late replies, and corrected callback ordering. Phone 390px, tablet 820px, and laptop 1440px layouts were visually inspected; suggestions stay bounded and controls are at least 48px. The unchanged request timeout was exercised for approximately 30 seconds. These checks do not prove Google authentication or durable backend writes.
- The separate private synthetic workbook began with **8 baseline student/history records** and **6 preserved legacy tabs**. Local fixture renders and native data readback were checked. Native baseline visual sanity passed under the exact Ops account across all eight tabs, including headers and populated fixture rows, duplicate names, unknown rank, blanks, `?`, `Transplant`, and `Early 2014`. Final after-save native Students and History inspection also passed: nine student rows and all twelve new event rows visibly retained the expected dates, ranks, and references. Final independent preservation checks passed as detailed below.
- The actual version-2 hosted app passed laptop 1440px, tablet 820px, and phone 390px browser layout checks, with readable forms, before/after previews, history, and keyboard interaction. Fresh hosted documents showed TEST Alex's final one-stripe rank with both the original event and correction retained, and TEST Quinn's White Belt zero-stripe rank with registration and rank-confirmation history. Final app console errors were empty; the viewport was restored and existing tabs preserved. These are actual deployed-app checks; the earlier mocked browser evidence remains separately labeled.

Task-private evidence remains outside the repository: `work/promotions-final-tests.log`, `work/promotions-ui-qa/report.json`, the initial `work/promotions-fixture/native-verification.json`, and the final independent proof `final-native-verification-private.json`. Earlier failing checkpoints are superseded by the explicitly recorded recovery and final evidence. Private connection configuration must remain outside source, screenshots, and public evidence.

## Real hosted TEST checkpoint

Andrew completed the approved Google consent for **GYM IN A BOX Promotions — OPS PRIVATE TEST** under the dedicated Ops account. The scope is primary-email access plus see/edit/create/delete access to all Sheets accessible to that account, including live operational spreadsheets. Do not request identical consent again. Consent authorizes neither additional sharing nor live data edits. The existing workbook and replacement project are Ops-owned, and private owner/workbook properties remain aligned. Exact emails, project/workbook/deployment identifiers, URLs, and backend hashes stay in private configuration.

The actual `/exec` deployment is now **version 2**, at the same private URL, verified at the 12:03 PM staging checkpoint with owner-only access and `USER_ACCESSING` execution. Its backend matches `9c73042`; the unchanged interface and manifest retain the reviewed behavior and New York timezone. The old browser-consent-page blocker is resolved. No owner action is currently needed. Leave the superseded wrong-account project and authorization session untouched; its earlier sharing request remains withdrawn. Do not create another project, add sharing, switch accounts, or restart existing sessions.

The first real TEST Alex stripe was durably appended once, but native Sheets consumed the initial apostrophe in its carried legacy reference. The strict readback failed, and the old UI response incorrectly said Not saved. The minimal code fix preserves native literal escaping and treats post-append or retry-read failures as uncertain confirmation; it does not weaken historical validation. Only the damaged TEST `Promotion History!Y10` reference metadata was restored through native string-value entry. Every other cell, all six legacy tabs, the original eight history rows, and the appended event/request identities, rank, and date were preserved. Retrying the **same original UI request** recovered that original event: nine history events, excluding the header, at that recovery checkpoint, with the derived Students view correct and an independent fresh session confirming the persisted result. This is real post-commit failure/retry evidence. An intentional network-drop test has **not** been performed.

All real hosted workflows are complete; no save remains in flight:

- TEST Alex: one-to-two stripes, then an audited correction back to one, preserving both events.
- TEST Taylor: Black Belt five-to-six degrees without an ordinary stripe limit.
- TEST Rowan: Purple Belt four-to-five stripes, then Brown Belt with zero stripes and prior history intact.
- Duplicate-name TEST Jordan records: Morning White Belt zero-to-one stripe; Evening White Belt two-to-three stripes, as separate students.
- TEST Casey: explicit unknown-rank confirmation to White Belt two stripes, then a promotion to three.
- Missing TEST Quinn: explicit registration with unknown rank; a repeat name/label registration was rejected; current rank was then confirmed as White Belt zero stripes.
- TEST Ellis: one-to-two stripes saved in the current tab; a second tab's stale revision-one submission was rejected as changed in a different session, and Refresh showed two stripes.
- TEST Morgan: archived record remained read-only, with promotion controls absent and history available.

## Final independent verification

**PASS: 9 Students and 20 History events, comprising 8 unchanged baseline events and 12 hosted events.** Event/request identities are unique and match the expected sequences and revisions. Every hosted event has the verified Ops recorder, New York date `2026-09-13`, valid UTC timestamp and request fingerprint, and the expected before/after rank and approver. The correction's link and reason match exactly. The derived Students view agrees with the authoritative history. Its text-form boolean display was observed and is not a failure or an alternate source of rank authority.

The original eight History rows retain their native cell values, types, and formats. The first repaired stripe and its correction remain unchanged against their recorded checkpoints. All six legacy tabs retain their full grids, types, formulas, formats, and properties; original student legacy references match exactly. The workbook remains solely Ops-owned, unshared, and configured for New York time. No extra students or events were created by rejected duplicate/stale attempts or by recovery.

## Limits and release boundary

The genuine uncertain-save test was the actual post-commit readback failure and same-original-request recovery, including the precisely limited one-cell TEST metadata repair described above. No intentional network-drop test was performed. Browser viewport checks are not a physical phone/tablet hardware pass. Anonymous denial was checked against the real hosted app; signed-in nonowner and blank-identity handling have automated coverage, not a wrong-account browser pass.

Protected TEST verification is complete and the tool is ready for review, with no further owner action or test assignment required to finish QA. Production permission policy, live migration, and any production promotion release remain unapproved future decisions. PR #85 stays draft and unmerged. Preserve both released kiosks, the live promotion workbook, the superseded project, and existing account sessions. No additional sharing or live operational edits are authorized.
