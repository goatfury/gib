# Revolution manager-review release preparation — not authorized

**September 25 checkpoint:** the proposed live session remains paused. Stu's two confirmed requests are being implemented only in Revolution TEST: a capture-only daily attendance digest and reviewed recovery of forgotten Staff Clock finishes. See [the TEST operating notes](m1-attendance-digest-staff-recovery-test.md). The historical approval and release sequence below does not authorize or cover those new features. PR89 stays draft and unmerged; no live release, recipient configuration, new access or session with Stu is approved.

PR89 remains draft and unmerged. Andrew's current instruction authorizes Revolution TEST work only: no live release, permission change or session with Stu is authorized. The steps below are retained release preparation, not an approval request. The final PR receipt identifies the tested candidate commit, TEST deployment and receiver version; a later explicit approval must name those reviewed sources before any live action.

## Proposed approval

One supervised Revolution session with Stuart Turner (Stu), using his existing Admin access, reviewing the payroll period ending **October 4, 2026**. Cleanup remains visible from September 7, including unresolved older days. This does not approve an unattended or ongoing rollout after that session. Andrew retains existing access. Richmond access and deployments, Staff Clock, PR86 and both Walter records stay unchanged. Walter's correction remains for Stu and is not a release verification action.

The approval would cover merging the exact reviewed PR89 candidate, the receiver-first Revolution production release below, the separate Google permission handoff, and explicit activation of the manager screen and aggregate tablet badge for that session. It includes both recent fixes: independent Daily Review access after a failed manager load and original-save recovery after a lost addition confirmation. It would not authorize synthetic live records, migrating TEST data, changing payroll rules, granting access, performing Walter's correction, or wider rollout.

## Exact code and isolation

- TEST builds retain `GIB_M1_MANAGER_REVIEW_PILOT=true`. Live builds require the separate `GIB_M1_MANAGER_REVIEW_LIVE_PILOT=true`, `GIB_M1_INSTALLATION=rev` and `CONTEXT=production`. Both flags together, live Richmond, or live preview builds fail. Generated defaults remain disabled.
- The receiver's production wrapper is dormant unless its existing Script Properties explicitly contain `GIB_M1_MANAGER_REVIEW_LIVE_PILOT=active`. The persisted production target lock, exact Revolution Sheet identity and existing server authentication remain required.
- Production requests require the exact `gib-live` site identity, currently published production context and `https://gib-live.netlify.app`. TEST artifacts, other origins and Richmond cannot open production manager data.
- TEST callbacks remain fixed at `https://deploy-preview-89--gib-live.netlify.app/api/m1-test-read-result`. Production callbacks are fixed at `https://gib-live.netlify.app/api/m1-manager-read-result`. Request bodies cannot select destinations. Production uses a distinct signed schema, `production/rev/` keys and `gib-m1-manager-read-callback-production-v1` temporary store. Nothing is copied from TEST storage or Sheets.
- Callback transport applies to initial Admin reads, aggregate badge reads and a separate read-only original-addition proof. Browser start/status calls resume one centrally persisted ticket with one Google dispatch; they do not hold a browser response open for the full read. The existing 50-second observation budget and 60-second expiry stay fixed. Save writes, pre-save validation and review-save reconciliation retain their original paths. Full attendance and addition proofs stay behind existing Admin authentication; the badge returns only aggregate count/status and nonpersonal request status. Expired, conflicting, incomplete and failed reads remain unavailable.
- The proof endpoint, transport experiment headers, editor fault controls and expiring diagnostic receipts remain TEST-only. Live corrections open the existing Daily Review correction/removal tools for the selected date. The weaker synthetic-record manager VOID action stays TEST-only. Live additions reuse existing correction-compatible permanent IDs.
- `/api/m1-admin-add-check` is read-only and shares the existing manager activation boundary. Its trusted environment must match the exact canonical Revolution origin, published site and installation; request data cannot choose TEST or production. It uses existing Admin authentication and request proof, requires the original date-bound `m1-...` ID live, and never dispatches an addition or removal. Production reads the existing versioned removal-aware Daily Review envelope so pending removals, duplicate IDs and conflicting/VOID audits cannot be hidden. TEST keeps its separate synthetic-record contract.
- Interrupted Revolution instructor additions are checked automatically after uncertainty, reopening and a recovered connection. Recovery persists a separate read ticket alongside the unchanged original save before sending; reload resumes that ticket. The private `adminAdditionCheckRead` proof binds every original field, reviewer, date and environment, and reads the exact record, matching audit and manager ledger under one attendance lock. The lock is released before callback delivery. Existing strict receipt and fresh calendar/review validation must all pass before editing unlocks. An ordinary callback with `check:null` is never save evidence. Incomplete or mismatched evidence retains the original journal and blocks edits. An expired read can renew once on reopening or an actual recovered connection, not through an endless timer. **Check original save** remains available; **Retry original save** remains a separate deliberate same-ID write if the complete proof cannot confirm the original. Never create a replacement request. Lost duplicate (`alreadyexists`) receipts and uncertain partial/complete review saves retain their existing same-request reconciliation; the automatic addition proof must not infer their success.
- **Existing Daily Review tools** remains available after a failed first manager load or refresh, but loads its own fresh records before allowing corrections. An unresolved manager or Daily Review save blocks further edits until that original operation is reconciled. If neither read works, unfinished days remain pending and the screen reports unavailable.
- Class decisions, partial progress and day completion retain their original request across interruption and reload. **Retry / check the same save** first checks the original request against central review history; only a confirmed absence may proceed through the existing save validation. Both server and browser require `saved:true`, the exact original request ID and original revision plus one before releasing the pending save. A later review revision does not invalidate a valid receipt for the original operation. Incomplete or mismatched receipts remain unconfirmed and cannot trigger a replacement save. These review actions retain the deliberate same-save Retry flow; the automatic instructor-addition recovery does not apply to them.
- Partial progress may retain an existing cancellation that now conflicts with a late sign-in. The recorded teaching and visible conflict remain, and the day stays pending. This does not permit a new cancellation over recorded teaching, resolving upcoming classes, or completing an unresolved day. Recovery buttons remain disabled while the initial read is running and become available after either read outcome.

## Permission handoff, after approval only

The prepared **production** Apps Script manifest adds `https://www.googleapis.com/auth/script.external_request`. Google describes this as permission to connect to external services. The scope is broad at the provider level; this code sends authenticated read results only to the fixed Revolution callback above. Existing Drive/Sheets scopes and execute-as/access settings stay unchanged. TEST already has this permission; this pass neither grants nor requests production consent.

No new Netlify token, Admin role, Stu account, Richmond permission or secret is required. Existing production receiver/action credentials remain server-side and separate from TEST.

After release approval, Codex opens the existing production receiver in the existing Chrome window, verifies its project identity against the configured Revolution production receiver, and surfaces the exact authorization page. Andrew approves Google's new external-service permission himself. Codex does not accept consent silently or ask for secrets in chat. Stop if Google requests any additional scope beyond the existing scopes plus `script.external_request`.

## Receiver-first deployment sequence for Codex

1. Freeze the approved PR89 head and confirm required CI. Recheck published production checkpoints: Revolution Netlify `6aad7f89edef4d000822cf60`, source `c0809db06db9da12e249b52c24c49584d8e6c883`, production receiver **v8**; Richmond `6aa6ac11e275bbfe21c7c787`. If these changed, compare and retain the new rollback checkpoint before proceeding. Never restore old Sheets.
2. Prepare the existing Revolution production Apps Script project from exactly five reviewed sources: `production/Code.gs` as `Code.gs`, shared `GibM1Receiver.gs`, `GibM1ManagerReview.gs`, `GibM1TestReadCallback.gs`, and `production/appsscript.json` as `appsscript.json`. The production `.claspignore` allow-list contains only these files. Verify source hashes and exact existing project/deployment identity; do not run provisioning, create a Sheet/project, copy Script Properties, or upload TEST `Code.gs`.
3. Save the reviewed receiver source while the live manager activation property remains absent. After release approval, set that property to `active` and run only `authorizeRevolutionManagerReadCallback` to surface the separate Google consent handoff (its `getRequest` sends nothing). Andrew completes consent; then publish a new version to the **existing** Revolution deployment ID/URL. The currently deployed v8 ignores the new activation property until the new receiver is published. Record version and hashes. Keep the old UI. Confirm ordinary Daily Review read/authentication and existing VOID-audit compatibility before continuing.
4. Configure `GIB_M1_MANAGER_REVIEW_LIVE_PILOT=true` specifically in the Revolution Netlify **production build context**, leaving `GIB_M1_MANAGER_REVIEW_PILOT` unset there. Do not change the preview's TEST flag or any Richmond setting. Build the same approved source with that production configuration. Verify generated browser/server targets are both `production`, the existing installation profile is unchanged, and the upload client's function manifest retains all routes, Node runtime and streaming metadata, including `/api/m1-admin-add-check`. Confirm the artifact includes both the failed-load Daily Review fallback and original-save recovery. The result handler must expose both callback paths while runtime scope denies the TEST path on live. Preserve existing function routes and receiver environment configuration.
5. Because main automatically publishes Revolution, do not merge until this production build configuration, receiver checkpoint and rollback are ready. With separate explicit release approval, mark/merge the exact reviewed PR and let the existing Revolution deployment route publish it; verify the resulting merge tree and deployed source against the frozen candidate. Do not deploy Richmond. A detached candidate or preview must never be treated as a production-capable runtime.
6. Confirm actual published deployment ID/runtime, canonical Admin authentication, aggregate-only badge and one authoritative live manager read. Compare its dates/counts with a read-only authoritative snapshot and check actual serialized payload stays below the existing 240,000-character ledger / 256,000-byte callback limits. No fake instructor, correction, review confirmation or Staff Clock pairing is created for verification. Use existing approved tablet/session for any live kiosk check.
7. Stu performs the first real review as normal operations, with his existing identity and a genuine reason for any correction. Confirm central receipt before calling a save successful; preserve and reconcile the original request after any uncertain addition using **Check original save**. A matching record alone is insufficient: require its matching audit and fresh review status before editing resumes. If needed, **Retry original save** sends only the retained original request. Do not deliberately interrupt a real save for testing. Check the actual payroll export against authoritative records before payroll. This human operation is not replaced by a fabricated production test. At session end, stop new edits, reconcile any pending operation, then turn the pilot off as below unless Andrew separately approves continuing it.

## Evidence and unresolved risks

The release patch changes environment boundaries, callback routing and live correction handoff. Focused regression tests exercise production-mode handlers with synthetic credentials, cross-environment replay, expiry/conflicts, nonfatal scoped cleanup, authentication, aggregate privacy, TEST-only diagnostics, production audit compatibility, legacy correction IDs and target-bound UI responses. Hosted TEST checks exercise the changed shared callback path and failure/recovery. Existing evidence for unchanged attendance, CSV, cancellation, concurrency, queue recovery and upcoming-class guards is retained rather than rerun broadly.

A read-only production volume check on September 24 found 346 attendance rows, 132 since cleanup began, 131 active, and no Manager Reviews journal yet. A conservative serialization estimate was about **73.4 KB** before review snapshots, below the callback cap. This is sizing evidence, not an executed production callback; the first live read and later snapshot growth still require the release checks above. No production rows were changed or copied to TEST.

Three delivery failures must be assessed separately:

- **Missing callback:** request `93902e9b…` had persisted pending storage, successful schedule dependencies and Google HTTP302 in about3.2s, but no callback invocation/result. Google completed without the later detailed receipt. Missing evidence is that exact failed execution's validation/read outcome and outbound callback HTTP/acknowledgment result.
- **Browser cutoff:** a browser HTTP504 occurred at30.919s while the old server response waited until50.001s. The prepared repair removes that long-response dependency using short status calls for one persisted read, with the same total deadline. A delayed callback can be collected by a later invocation. This does not explain the platform's original cutoff or cause a missing callback to arrive; report hosted delayed-result verification separately.
- **Save reply failure:** both captured `ADMIN_ADD_HTTP_FAILURE` responses for `m1-2026-09-23-5855be82243ec61c311dac0b` were non-2xx Google HTTP replies before JSON/receipt validation. The underlying upstream status/hop was not retained; there is no evidence a valid receipt was rejected. Its one record and audit were preserved and the original save recovered. New read-only addition confirmation uses the authenticated callback, avoiding dependence on that ordinary Google reply while retaining full proof. The write itself is unchanged. Production-mode proof is tested only with isolated fake data; the canonical live callback remains unverified and off. Report hosted interrupted-save/reload results with exact row/audit counts in the final TEST receipt.

None of this evidence establishes production reliability or authorizes a pilot. A genuine missing callback, unavailable calendar dependency, failed audit proof or prolonged connection outage can still interrupt work safely. Existing Daily Review remains a fallback only after its own fresh read; an unresolved save blocks further edits in both screens. The production permission/canonical callback path remains unverified until a separately approved release. No open-ended failure hunt, longer deadline or automatic write retry is proposed.

## Pilot stop conditions and rollback

- One unavailable read leaves review controls unavailable. Allow one deliberate Refresh. Two unavailable reads within five minutes stop manager pilot work; use existing Daily Review and capture the existing sanitized request IDs/times. Do not start a retry loop.
- Stop immediately for any wrong gym/environment, missing or duplicated instructor, incorrect VOID/export behavior, missing audit, false all-clear, or unexpectedly changed permission. Also stop if an uncertain save cannot be confirmed with its preserved original request. Do not submit a replacement request to bypass uncertainty.
- To turn off after the supervised session, stop new edits and reconcile pending operations first. Unset `GIB_M1_MANAGER_REVIEW_LIVE_PILOT` in the Revolution production build context and rebuild/redeploy the reviewed source, or restore the retained pre-pilot Netlify checkpoint. This is a **build-time switch**: changing the variable alone does not change an already-published UI. Verify both generated manager targets are disabled, the normal Daily Review is available and `/api/m1-admin-add-check` denies manager recovery. Then remove the receiver's live activation property. Keep receiver support for any unresolved original operations until reconciled; never clear their journals to turn the feature off.
- Disable new manager work before rollback. Reconcile any pending save/correction with the original request and retain local queues. Restore the retained Revolution Netlify checkpoint and keep a receiver version capable of confirming pending operations and reading all existing VOID/audit history. Coordinate automatic main deployments so they cannot overwrite the rollback.
- Once pending operations are resolved, remove the manager activation property and, if needed, restore receiver v8 after checking its retained removal/audit compatibility. Retain Manager Reviews, permanent IDs, VOID statuses, operation notes and Admin Audit permanently. Rollback is code/configuration only: never delete journals, replay TEST data, restore an old workbook, clear a tablet, or roll back Richmond.

Trey's missing existing Admin access remains a Richmond rollout prerequisite and is outside this Revolution pilot.
